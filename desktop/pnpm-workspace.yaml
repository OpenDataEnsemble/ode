# pnpm 11: dependency lifecycle scripts are blocked unless listed here.
# Run `pnpm approve-builds` locally to update; commit this file so CI matches.
allowBuilds:
  '@nestjs/core': true
  '@openapitools/openapi-generator-cli': true
  esbuild: true
