# Custodian — task log

Short-lived backlog items that are not worth a full roadmap file.

## Completed

- **OS keyring fallback (Linux / headless):** Credential commands return structured results (`CredentialGetResult`, `CredentialSetResult`, `CredentialDeleteResult`). Profiles saves **profile data even when the password cannot be stored**; the user sees an explicit warning. See [SCREEN_REFERENCE.md](./SCREEN_REFERENCE.md) → Profiles → “Secure storage fallback”.

## Open

- **Structured UI review:** Use [SCREEN_REFERENCE.md](./SCREEN_REFERENCE.md) as the checklist; produce keep/remove/defer notes per screen.
